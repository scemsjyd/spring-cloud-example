spring:
  application:
    name: spring-cloud-example-hystrix
server:
  port: 8004
feign:
  hystrix:
    enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"